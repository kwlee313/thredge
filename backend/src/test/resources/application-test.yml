spring:
  datasource:
    url: jdbc:h2:mem:thredge_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always
